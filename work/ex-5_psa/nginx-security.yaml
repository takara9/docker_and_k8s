apiVersion: v1
kind: Pod
metadata:
  name: nginx-secure
  namespace: myns
spec:
  containers:
    - image: ghcr.io/takara9/sec-nginx:1.0
      name: nginx
      ports:
        - containerPort: 9200
      securityContext:
        allowPrivilegeEscalation: false
        runAsNonRoot: true
        capabilities:
          drop:
            - ALL
        seccompProfile: 
          type: RuntimeDefault
        runAsUser: 101
        runAsGroup: 101
